# Phase 1 - Foundation
1. Install deps: react-hook-form, @hookform/resolvers, zod, @tanstack/react-table, date-fns
2. Create admin types + mock data
3. Build admin UI primitives (Table, Dialog, Select, Tabs, DropdownMenu, Sheet, Tooltip, Switch, Textarea, Checkbox, Separator, Label, Avatar)
4. Build shared components (PageHeader, TierBadge, GameBadge, StatusBadge, EmptyState, ConfirmDialog, DataCard, SearchInput)
5. Build layout (Sidebar, DashboardHeader, UserMenu, DashboardLayout)
6. Create admin routes (layout + page stubs)
